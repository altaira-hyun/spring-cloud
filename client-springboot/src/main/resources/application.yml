server:
  port: 8030

spring:
  application:
    name: spring-service
  cloud:
    config:
      uri: http://config-server-svc.spring-cloud.svc.cluster.local:8080
  config:
    import: "optional:configserver:"
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://mysql-svc.spring-cloud.svc.cluster.local:9009/example?serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password: Megazone00!
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://service-discovery-0.service-discovery-svc.spring-cloud.svc.cluster.local:8761/eureka,http://service-discovery-1.service-discovery-svc.spring-cloud.svc.cluster.local:8761/eureka
